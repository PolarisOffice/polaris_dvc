name: DVC parity (Windows)

# Build upstream hancom-io/dvc on a Windows runner following the
# **exact recipe** from upstream's README, run the resulting binary
# against every fixture under `testdata/golden/`, and publish the
# binary set as a reusable artifact.
#
# Prior iterations of this workflow piled up workaround after
# workaround (v143 retarget, x86 fallback, vcpkg jsoncpp, PCH sed
# patches, vcxproj path rewrites). Each was needed because the build
# didn't exist — yet each moved us further from what upstream devs
# actually ship. The result was binaries that loaded cleanly but
# crashed inside `setCommand(argv)` with 0xC0000005 whenever the user
# passed any arguments, almost certainly from ABI skew between DVC's
# compile-time jsoncpp headers and whatever runtime jsoncpp.dll we'd
# stuffed in.
#
# This rewrite matches upstream's published build (README):
#   - Visual Studio 2017 (v141) toolset — installed on top of the
#     runner's VS2022 via VS Installer
#   - Win64 (x64) platform
#   - jsoncpp built from source as `jsoncpp_lib` (shared DLL) via
#     CMake, left at the paths DVCModel.vcxproj expects by default
#   - Zero vcxproj patching — if the binary doesn't build this way,
#     upstream shouldn't be able to build it either, and we learn
#     something real.

on:
  workflow_dispatch:
    inputs:
      upstream_ref:
        description: "Upstream hancom-io/dvc ref (branch/tag/SHA)"
        default: "main"
        required: false
  push:
    branches: [main]
    paths:
      - ".github/workflows/dvc-parity.yml"
      - "scripts/parity-windows.ps1"
      - "scripts/diff-dvc-outputs.sh"

permissions:
  contents: read

jobs:
  build:
    name: Build DVC binary set (v143 / x64, near-upstream recipe)
    runs-on: windows-latest
    env:
      UPSTREAM_REF: ${{ inputs.upstream_ref || 'main' }}
    outputs:
      artifact_name: ${{ steps.publish.outputs.artifact_name }}
    steps:
      - name: Checkout upstream hancom-io/dvc
        uses: actions/checkout@v4
        with:
          repository: hancom-io/dvc
          ref: ${{ env.UPSTREAM_REF }}
          path: dvc

      - name: Fetch OSS dependencies
        shell: bash
        # Upstream's `update_oss.bat` requires a GitHub token argument
        # and has a typo on the jsoncpp reset (line 58: `cd rapidjson`
        # instead of `cd jsoncpp`) that silently leaves jsoncpp
        # unpinned. We replicate the *intent* (clone + pin rapidjson,
        # clone jsoncpp at HEAD) without the bat's bugs and without
        # a token.
        run: |
          set -euo pipefail
          cd dvc
          mkdir -p opensource
          cd opensource
          echo "::group::hwpx-owpml-model"
          git clone --depth 1 https://github.com/hancom-io/hwpx-owpml-model.git
          echo "::endgroup::"
          echo "::group::rapidjson"
          git clone https://github.com/Tencent/rapidjson.git
          git -C rapidjson reset --hard 8261c1ddf43f10de00fd8c9a67811d1486b2c784
          echo "::endgroup::"
          echo "::group::jsoncpp"
          # Matches upstream's effective behavior (bat pins but fails
          # silently). Using HEAD keeps our build on the same codebase
          # upstream devs would land on running update_oss.bat today.
          git clone --depth 1 https://github.com/open-source-parsers/jsoncpp.git
          echo "::endgroup::"

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Detect installed Windows SDK
        id: sdk
        shell: pwsh
        run: |
          $root = 'C:\Program Files (x86)\Windows Kits\10\Include'
          if (!(Test-Path $root)) { $root = 'C:\Program Files\Windows Kits\10\Include' }
          $versions = Get-ChildItem $root -Directory |
            Where-Object { $_.Name -match '^10\.' } |
            Sort-Object Name -Descending
          if ($null -eq $versions -or $versions.Count -eq 0) {
            throw "No Windows 10 SDK found under $root"
          }
          $ver = $versions[0].Name
          Write-Host "Using Windows SDK: $ver"
          "ver=$ver" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Build jsoncpp as shared lib (CMake / x64)
        shell: pwsh
        # Upstream README:
        #   opensource\jsoncpp\cmake CMakeLists.txt -B json_git -G "Visual Studio 15 2017 Win64"
        #   open jsoncpp.sln → build jsoncpp_lib project → confirm jsoncpp.dll + lib
        #
        # x64 via `-A x64`. Shared DLL via BUILD_SHARED_LIBS=ON, which
        # matches the README's `jsoncpp_lib` target. We use the
        # runner's default toolset (v143) — tried Path A (install
        # v141 matching upstream's README) but VS Installer modify
        # failed with exit 1 on windows-latest; falling back to v143
        # + x64 + shared jsoncpp. If this still crashes inside
        # setCommand, we'll need a more involved v141 install path.
        run: |
          Push-Location dvc\opensource\jsoncpp
          cmake -G "Visual Studio 17 2022" -A x64 -B json_git `
                -DJSONCPP_WITH_TESTS=OFF `
                -DJSONCPP_WITH_POST_BUILD_UNITTEST=OFF `
                -DBUILD_SHARED_LIBS=ON `
                -DJSONCPP_STATIC_WINDOWS_RUNTIME=OFF
          cmake --build json_git --config Release --target jsoncpp_lib
          Write-Host "---"
          Write-Host "jsoncpp build outputs:"
          Get-ChildItem -Recurse -Path json_git -Include *.dll,*.lib |
            Select-Object FullName, Length |
            Format-Table -AutoSize
          Pop-Location

      - name: Build DVCModel.sln (Release / x64)
        shell: cmd
        # Straight msbuild call with upstream's expected platform. No
        # sed patches, no path rewrites — DVCModel.vcxproj finds
        # jsoncpp via `./opensource/jsoncpp/include/json` (headers)
        # and `./opensource/jsoncpp/json_git/lib/Release` (import
        # lib) by default, both of which the previous step populates.
        # Toolset intentionally left at vcxproj default (gets auto-
        # upgraded to v143 by msbuild since v141 isn't installed).
        run: |
          cd dvc
          msbuild DVCModel.sln ^
            /p:Configuration=Release ^
            /p:Platform=x64 ^
            /p:PlatformToolset=v143 ^
            /p:WindowsTargetPlatformVersion=${{ steps.sdk.outputs.ver }} ^
            /m /v:minimal

      - name: Locate build output
        id: locate
        shell: pwsh
        # Release / x64 output dir differs across vcxprojs: the root
        # `DVCModel.vcxproj` emits to `dvc/x64/Release/`, while older
        # configs emit to `dvc/Release/`. Find the exe dynamically so
        # the rest of the pipeline doesn't care.
        run: |
          $exeItem = Get-ChildItem -Path dvc -Recurse -Filter ExampleWindows.exe -File |
            Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if (-not $exeItem) { throw "ExampleWindows.exe not found after build" }
          $dllItem = Get-ChildItem -Path dvc -Recurse -Filter DVCModel.dll -File |
            Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if (-not $dllItem) { throw "DVCModel.dll not found after build" }
          $stageDir = Split-Path -Parent $exeItem.FullName
          Write-Host "stage dir: $stageDir"
          Write-Host "exe:       $($exeItem.FullName)"
          Write-Host "dll:       $($dllItem.FullName)"
          "stage_dir=$stageDir" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "exe_path=$($exeItem.FullName)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Stage jsoncpp.dll next to ExampleWindows.exe
        shell: pwsh
        run: |
          $stage = "${{ steps.locate.outputs.stage_dir }}"
          $src = Get-ChildItem -Path dvc\opensource\jsoncpp\json_git -Recurse `
                               -Filter jsoncpp.dll -File |
                 Select-Object -First 1
          if (-not $src) { throw "jsoncpp.dll not found — shared-lib build must have failed" }
          Copy-Item $src.FullName $stage -Force
          Write-Host "staged: $($src.FullName) -> $stage"
          Write-Host "---"
          Write-Host "Final $stage contents:"
          Get-ChildItem $stage | Select-Object Name, Length | Format-Table -AutoSize

      - name: Inspect DLL dependency tree (dumpbin /dependents)
        shell: pwsh
        run: |
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          # x64 dumpbin for x64 binaries.
          $dumpbin = & $vswhere -latest -find 'VC\Tools\MSVC\*\bin\Hostx64\x64\dumpbin.exe' | Select-Object -First 1
          if (-not $dumpbin) {
            Write-Host "dumpbin not found; skipping dependency inspection"
            return
          }
          Write-Host "dumpbin: $dumpbin"
          $stage = "${{ steps.locate.outputs.stage_dir }}"
          foreach ($f in @("ExampleWindows.exe", "DVCModel.dll", "jsoncpp.dll")) {
            $p = Join-Path $stage $f
            if (Test-Path $p) {
              Write-Host "::group::$f dependencies"
              & $dumpbin /dependents $p
              Write-Host "::endgroup::"
            }
          }

      - name: Smoke-test ExampleWindows.exe (no args → prints Hello World)
        shell: cmd
        continue-on-error: true
        run: |
          "${{ steps.locate.outputs.exe_path }}"
          echo exit_code=%errorlevel%

      - name: Publish binary artifact
        id: publish
        uses: actions/upload-artifact@v4
        with:
          name: dvc-windows-x64-${{ github.run_id }}
          path: ${{ steps.locate.outputs.stage_dir }}/**
          retention-days: 90
          if-no-files-found: error

      - name: Set artifact output
        shell: bash
        run: echo "artifact_name=dvc-windows-x64-v141-${{ github.run_id }}" >> "$GITHUB_OUTPUT"

      - name: Upload build logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: msbuild-logs-${{ github.run_id }}
          path: |
            dvc/**/*.log
            dvc/**/BuildLog.htm
          if-no-files-found: ignore

  parity:
    name: Run DVC against golden fixtures
    needs: build
    runs-on: windows-latest
    steps:
      - name: Checkout polaris
        uses: actions/checkout@v4

      - name: Download DVC binary artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.artifact_name }}
          path: dvc-bin

      - name: List artifact contents
        shell: pwsh
        run: Get-ChildItem -Recurse dvc-bin | Select-Object FullName, Length | Format-Table -AutoSize

      - name: Single-fixture probe (cmd.exe)
        shell: cmd
        continue-on-error: true
        # If this prints "Hello World!" + produces an output JSON, we
        # have parity. If it crashes, next step is to attach debug
        # symbols or rebuild upstream as Debug config.
        run: |
          for /f "delims=" %%X in ('dir /b /s "dvc-bin\ExampleWindows.exe"') do set EXE=%%X
          echo using: %EXE%
          set DOC=testdata\golden\01_clean\doc.hwpx
          set SPEC=testdata\golden\01_clean\spec.json
          set OUT=testdata\golden\_dvc-output\01_clean.probe.json
          mkdir testdata\golden\_dvc-output 2>nul
          echo --- running with args ---
          "%EXE%" -j "--file=%OUT%" -t "%SPEC%" "%DOC%"
          echo exit_code=%errorlevel%
          if exist "%OUT%" (echo --- output file produced --- & type "%OUT%") else (echo no output file)

      - name: Run DVC against each golden fixture
        shell: pwsh
        run: |
          $exeItem = Get-ChildItem -Path dvc-bin -Recurse -Filter ExampleWindows.exe | Select-Object -First 1
          if (-not $exeItem) { throw "ExampleWindows.exe not found under dvc-bin/" }
          $exe = $exeItem.FullName
          Write-Host "Using exe: $exe"
          $outDir = "testdata\golden\_dvc-output"
          New-Item -ItemType Directory -Path $outDir -Force | Out-Null
          $cases = Get-ChildItem "testdata\golden" -Directory |
            Where-Object { $_.Name -ne "_dvc-output" -and (Test-Path (Join-Path $_.FullName "doc.hwpx")) }
          Write-Host "Processing $($cases.Count) fixtures"
          foreach ($case in $cases) {
            $name = $case.Name
            $doc = Join-Path $case.FullName "doc.hwpx"
            $spec = Join-Path $case.FullName "spec.json"
            $out = Join-Path $outDir "$name.json"
            Write-Host "::group::$name"
            $combined = & $exe -j "--file=$out" -t $spec $doc 2>&1
            $rc = $LASTEXITCODE
            Write-Host "exit_code=$rc"
            if ($combined) { Write-Host "--- output ---"; $combined | ForEach-Object { Write-Host $_ } }
            if (Test-Path $out) {
              Write-Host "--- output.json ---"
              Get-Content $out
            } else {
              Write-Host "(no output.json produced)"
            }
            Write-Host "::endgroup::"
          }

      - name: Soft-diff DVC output vs committed expected.json
        shell: bash
        continue-on-error: true
        run: |
          bash scripts/diff-dvc-outputs.sh testdata/golden testdata/golden/_dvc-output \
            | tee testdata/golden/_dvc-output/_diff-summary.txt

      - name: Upload DVC outputs + per-fixture logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dvc-parity-outputs-${{ github.run_id }}
          path: testdata/golden/_dvc-output/
          if-no-files-found: warn
